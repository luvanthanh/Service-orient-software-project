server:
  port: 8081
  servlet:
    context-path: /ProductDatabase

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ProductDatabase
    username: root
    password: "@Luvanthanh24072005"

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false