server:
  port: 8084
  servlet:
    context-path: /CartDatabase

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/CartDatabase
    username: root
    password: "@Luvanthanh24072005"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

